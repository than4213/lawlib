import Lawlib.Core.Money
import Lawlib.Core.Date
import Lawlib.Core.Entity
import Lawlib.Core.ExtRat
import Lawlib.Core.Scale
import Lawlib.Household
import Lawlib.Parameters
import Lawlib.USA.DOE
import Lawlib.USA.SSA
import Lawlib.USA.HUD
import Lawlib.USA.FCC
import Lawlib.USA.IRS
import Lawlib.USA.ED
import Lawlib.USA.ACA
import Lawlib.USA.General
import Lawlib.USA.USDA
import Lawlib.USA.HHS
import Lawlib.USA.SSA.Part2
import Lawlib.USA.HUD.Part2
import Lawlib.USA.IRS.Part2
import Lawlib.USA.ED.Part2
import Lawlib.USA.ACA.Part2
import Lawlib.USA.General.Part2
import Lawlib.USA.USDA.Part2
import Lawlib.USA.HHS.Part2
import Lawlib.USA.HUD.Part3
import Lawlib.USA.FCC.Part3
import Lawlib.USA.ED.Part3
import Lawlib.USA.USDA.Part3
import Lawlib.USA.HHS.Part3
import Lawlib.USA.ED.Part4
import Lawlib.USA.ACA.Part4

/-!
Generated by pe2lean 0.1.0 from policyengine-us 1.783.0.
DO NOT EDIT — regenerate with `pe2lean extract` (DESIGN.md §5).

Health and Human Services programs: Medicaid, CHIP, TANF, Medicare cost sharing.

Continues the module of the same name. The law
here quotes rules from other agencies, which Lean
must see first; the split is a compilation order,
not a distinction in the law.
-/

namespace Lawlib.USA
open Lawlib
open Lawlib.Parameters

set_option linter.unusedVariables false

set_option maxHeartbeats 1000000
set_option maxRecDepth 8192

/-- `policyengine_us/variables/gov/hhs/head_start/is_head_start_categorically_eligible.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isHeadStartCategoricallyEligible (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (((decide (((tanf t d) + ((ssi t p d) * 12) + ((snap t d) * 12)) > 0)) ||
      (decide ((boolToRat t.hhs.receivesTanf
  + boolToRat p.ssa.receivesSsi
  + boolToRat t.usda.receivesSnap) > 0))) || (t.core.isHomeless ||
      p.coreP2.wasInFosterCare))

/-- `medicaid_community_engagement_pass_through_eligible.py`
    in `policyengine_us/variables/gov/hhs/medicaid/eligibility/`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def medicaidCommunityEngagementPassThroughEligible (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  ((((((decide ((snap t d) > 0)) || t.usda.receivesSnap) && (!(decide
      ((usda.snap.work_requirements.general.age_threshold.exempted.atDate d (monthlyAge t p
      d)) ≠ 0)))) && (!(isSnapWorkRegistrationExemptNonAge t p d))) && (if (anyBy t.members
      fun p => (decide ((monthlyAge t p d) < (if p.usda.isSnapAbawdHr1InEffect then
      (usda.snap.work_requirements.abawd.age_threshold.dependent.atDate d) else
      (usda.snap.work_requirements.abawd.age_threshold.dependent.atDate ⟨2025, 6, 1⟩)))))
      then p.usda.meetsSnapGeneralWorkRequirements else
      (p.usda.meetsSnapGeneralWorkRequirements && p.usda.meetsSnapAbawdWorkRequirements)))
      || (((isTanfEnrolled t d) || t.hhs.receivesTanf) && (meetsTanfWorkRequirements t d)))

/-- `policyengine_us/variables/gov/hhs/head_start/is_early_head_start_eligible.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isEarlyHeadStartEligible (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (((decide (p.coreP1.age < (hhs.head_start.early_head_start.age_limit.atDate d))) ||
      p.coreP1.isPregnant) && ((isHeadStartIncomeEligible t p d) ||
      (isHeadStartCategoricallyEligible t p d)))

end Lawlib.USA
