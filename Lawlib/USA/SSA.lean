/-
Generated by pe2lean 0.1.0 from policyengine-us 1.783.0.
DO NOT EDIT — regenerate with `pe2lean extract` (DESIGN.md §5).

Social Security Administration programs, chiefly SSI.
-/
import Lawlib.Core.Money
import Lawlib.Core.Date
import Lawlib.Core.Entity
import Lawlib.Core.ExtRat
import Lawlib.Core.Scale
import Lawlib.Household
import Lawlib.Parameters
import Lawlib.USA.DOE

namespace Lawlib.USA
open Lawlib
open Lawlib.Parameters

set_option linter.unusedVariables false

set_option maxHeartbeats 1000000
set_option maxRecDepth 8192

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/status/is_ssi_aged.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSsiAged (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (decide (p.coreP1.age ≥ (ssa.ssi.eligibility.aged_threshold.atDate d)))

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/status/is_ssi_qualified_noncitizen.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSsiQualifiedNoncitizen (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (((ssa.ssi.eligibility.status.qualified_noncitizen_status.atDate d).contains
      (ImmigrationStatus.asStr p.coreP1.immigrationStatus)) &&
      ((!(p.coreP1.immigrationStatus == ImmigrationStatus.LEGAL_PERMANENT_RESIDENT)) ||
      (decide (p.coreP1.ssiQualifyingQuartersEarnings ≥
      (ssa.ssi.income.sources.qualifying_quarters_threshold.atDate d)))))

/-- `policyengine_us/variables/gov/ssa/ss/social_security.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def socialSecurity (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (p.ssa.socialSecurityDependents
  + p.ssa.socialSecurityDisability
  + p.ssa.socialSecurityRetirement
  + p.ssa.socialSecuritySurvivors)

/-- `policyengine_us/variables/gov/ssa/social_security/ss_aime_eligible.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def ssAimeEligible (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (decide (p.coreP1.age ≥ (ssa.social_security.aime.minimum_age_for_earnings.atDate d)))

/-- `policyengine_us/variables/gov/ssa/social_security/ss_claiming_age.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def ssClaimingAge (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  p.coreP1.age

/-- `policyengine_us/variables/gov/ssa/social_security/ss_covered_earnings_this_year.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def ssCoveredEarningsThisYear (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (min ((p.coreP1.employmentIncome + p.coreP2.totalSelfEmploymentIncome) : Rat)
      (ssa.social_security.wage_base.atDate d))

/-- `policyengine_us/variables/gov/ssa/social_security/ss_pia.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def ssPia (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (((ratFloor ((ssa.social_security.pia.formula_factors.marginalCalc d p.ssa.ssAime) * 10))
      : Rat) / 10)

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/resources/ssi_countable_resources.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def ssiCountableResources (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (p.coreP1.bankAccountAssets + p.coreP1.stockAssets + p.coreP1.bondAssets)

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/income/ssi_earned_income.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def ssiEarnedIncome (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (p.coreP1.employmentIncome
  + p.coreP1.selfEmploymentIncome
  + p.coreP1.sstbSelfEmploymentIncome)

/-- `ssi_marital_both_eligible.py`
    in `policyengine_us/variables/gov/ssa/ssi/eligibility/income/marital/`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def ssiMaritalBothEligible (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (decide ((if (p.coreP1.isTaxUnitHead || p.coreP1.isTaxUnitSpouse) then (sumBy t.members
      fun q => if (q.coreP1.isTaxUnitHead || q.coreP1.isTaxUnitSpouse) then
      (boolToRat q.ssa.isSsiAgedBlindDisabled) else 0) else (boolToRat
      p.ssa.isSsiAgedBlindDisabled)) = 2))

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/income/ism/ssi_pmv_applies.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def ssiPmvApplies (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  ((p.ssa.ssiFederalLivingArrangement == SSIFederalLivingArrangement.OWN_HOUSEHOLD) &&
      ((p.ssa.ssiReceivesOutsideShelterSupport || p.ssa.ssiReceivesShelterFromOthersInHousehold)
      || (p.ssa.ssiReceivesFoodFromOthers && (ssa.ssi.income.ism.food_counts.atDate d))))

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/status/is_ssi_eligible_spouse.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSsiEligibleSpouse (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (p.coreP1.isTaxUnitSpouse && (ssiMaritalBothEligible t p d))

/-- `policyengine_us/variables/gov/ssa/social_security/ss_retirement_eligible.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def ssRetirementEligible (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (decide ((ssClaimingAge t p d) ≥ (ssa.social_security.minimum_retirement_age.atDate d)))

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/income/ssi_engaged_in_sga.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def ssiEngagedInSga (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  ((decide (((ssiEarnedIncome t p d) / 12) > (ssa.sga.non_blind.atDate d))) &&
      (!p.coreP1.isBlind))

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/income/ssi_unearned_income.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def ssiUnearnedIncome (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (p.coreP1.pensionIncome
  + (socialSecurity t p d)
  + p.coreP1.disabilityBenefits
  + p.coreP2.veteransBenefits
  + p.coreP2.survivorBenefits
  + p.states.workersCompensation
  + p.states.unemploymentCompensation
  + p.coreP1.retirementDistributions
  + p.coreP1.giCashAssistance
  + p.coreP1.alimonyIncome
  + p.coreP1.childSupportReceived
  + p.coreP1.financialAssistance
  + p.coreP1.dividendIncome
  + p.coreP1.interestIncome
  + p.coreP1.rentalIncome)

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/status/is_ssi_disabled.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSsiDisabled (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (p.ssa.meetsSsiDisabilityCriteria && (!(ssiEngagedInSga t p d)))

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/status/is_ssi_eligible_individual.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSsiEligibleIndividual (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  ((p.ssa.isSsiAgedBlindDisabled && (!(isSsiEligibleSpouse t p d))) &&
      ((isSsiQualifiedNoncitizen t p d) ||
      (p.coreP1.immigrationStatus == ImmigrationStatus.CITIZEN)))

/-- `policyengine_us/variables/gov/ssa/social_security/ss_earnings_test_applicable.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def ssEarningsTestApplicable (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  ((ssRetirementEligible t p d) && (decide
      (p.coreP1.age < (p.ssa.ssFullRetirementAgeMonths / 12))))

/-- `ss_retirement_benefit_before_earnings_test.py`
    in `policyengine_us/variables/gov/ssa/social_security/`
    policyengine-us 1.783.0, entity person, value_type float. -/
def ssRetirementBenefitBeforeEarningsTest (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (if (ssRetirementEligible t p d) then
      (((ssPia t p d) * p.ssa.ssRetirementAgeAdjustmentFactor) * 12) else 0)

/-- `policyengine_us/variables/gov/ssa/social_security/ss_earnings_test_reduction.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def ssEarningsTestReduction (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  let fra_years := (p.ssa.ssFullRetirementAgeMonths / 12);
  let year_of_fra := ((decide (p.coreP1.age < fra_years)) && (decide
      ((p.coreP1.age + 1) ≥ fra_years)));
  (if (ssEarningsTestApplicable t p d) then (min (((max (0 : Rat)
      ((p.coreP1.employmentIncome + p.coreP2.totalSelfEmploymentIncome) - (if year_of_fra
      then (ssa.social_security.earnings_test.exempt_amount_year_of_fra.atDate d) else
      (ssa.social_security.earnings_test.exempt_amount_under_fra.atDate d)))) * (if
      year_of_fra then (ssa.social_security.earnings_test.reduction_rate_year_of_fra.atDate
      d) else (ssa.social_security.earnings_test.reduction_rate_under_fra.atDate d))) : Rat)
      (ssRetirementBenefitBeforeEarningsTest t p d)) else 0)

/-- `policyengine_us/variables/gov/ssa/ssi/eligibility/status/ssi_category.py`
    policyengine-us 1.783.0, entity person, value_type Enum. -/
def ssiCategory (t : TaxUnit) (p : Person) (d : Date) : SSICategory :=
  (if p.coreP1.isBlind then SSICategory.BLIND else (if (isSsiAged t p d) then
      SSICategory.AGED else
      (if (isSsiDisabled t p d) then SSICategory.DISABLED else SSICategory.NONE)))

end Lawlib.USA
