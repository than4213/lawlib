import Lawlib.Core.Money
import Lawlib.Core.Date
import Lawlib.Core.Entity
import Lawlib.Core.ExtRat
import Lawlib.Core.Scale
import Lawlib.Household
import Lawlib.Parameters
import Lawlib.USA.DOE
import Lawlib.USA.SSA
import Lawlib.USA.HUD
import Lawlib.USA.FCC
import Lawlib.USA.IRS
import Lawlib.USA.ED
import Lawlib.USA.ACA
import Lawlib.USA.General

/-!
Generated by pe2lean 0.1.0 from policyengine-us 1.783.0.
DO NOT EDIT — regenerate with `pe2lean extract` (DESIGN.md §5).

Agriculture Department programs, chiefly SNAP and school meals.
-/

namespace Lawlib.USA
open Lawlib
open Lawlib.Parameters

set_option linter.unusedVariables false

set_option maxHeartbeats 1000000
set_option maxRecDepth 8192

/-- `is_snap_employment_training_or_work_incentive_student.py`
    in `policyengine_us/variables/gov/usda/snap/eligibility/student/`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSnapEmploymentTrainingOrWorkIncentiveStudent (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (p.usda.isSnapEmploymentTrainingStudent || p.usda.isSnapWorkIncentiveStudent)

/-- `policyengine_us/variables/gov/usda/snap/eligibility/student/is_snap_higher_ed_student.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSnapHigherEdStudent (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (p.coreP1.isFullTimeCollegeStudent || p.coreP1.isPartTimeCollegeStudent)

/-- `policyengine_us/variables/gov/usda/snap/eligibility/is_snap_immigration_status_eligible.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSnapImmigrationStatusEligible (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (((usda.snap.eligibility.eligible_immigration_statuses.atDate d).contains
      (ImmigrationStatus.asStr p.coreP1.immigrationStatus)) ||
      p.statesCa.caSnapImmigrationStatusEligible)

/-- `policyengine_us/variables/gov/usda/is_usda_elderly.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isUsdaElderly (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (decide (p.coreP1.age ≥ (usda.elderly_age_threshold.atDate d)))

/-- `policyengine_us/variables/gov/usda/snap/eligibility/student/meets_snap_work_exception.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def meetsSnapWorkException (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  ((decide (p.coreP2.weeklyHoursWorkedBeforeLsr ≥
      (usda.snap.student.working_hours_threshold.atDate d))) ||
      p.ed.isFederalWorkStudyParticipant)

/-- `snap_allowable_medical_expenses.py`
    in `policyengine_us/variables/gov/usda/snap/income/deductions/`
    policyengine-us 1.783.0, entity person, value_type float. -/
def snapAllowableMedicalExpenses (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (p.coreP1.medicalExpenseHealthInsurancePremiums + p.coreP1.otherMedicalExpenses)

/-- `policyengine_us/variables/gov/usda/snap/eligibility/snap_assets.py`
    policyengine-us 1.783.0, entity spm_unit, value_type float. -/
def snapAssets (t : TaxUnit) (d : Date) : Rat :=
  ((sumBy t.members fun p => p.coreP1.bankAccountAssets)
  + (sumBy t.members fun p => p.coreP1.stockAssets)
  + (sumBy t.members fun p => p.coreP1.bondAssets))

/-- `policyengine_us/variables/gov/usda/snap/income/snap_countable_earner.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def snapCountableEarner (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (!(p.usda.snapExcludedChildEarner || p.ed.isFederalWorkStudyParticipant))

/-- `policyengine_us/variables/gov/usda/snap/income/gross/snap_gross_test_income_fpg_ratio.py`
    policyengine-us 1.783.0, entity spm_unit, value_type float. -/
def snapGrossTestIncomeFpgRatio (t : TaxUnit) (d : Date) : Rat :=
  (t.usda.snapGrossTestIncome / t.usda.snapFpg)

/-- `snap_self_employment_income_after_expense_deduction.py`
    in `policyengine_us/variables/gov/usda/snap/income/deductions/self_employment/`
    policyengine-us 1.783.0, entity spm_unit, value_type float. -/
def snapSelfEmploymentIncomeAfterExpenseDeduction (t : TaxUnit) (d : Date) : Rat :=
  (max (((sumBy t.members fun p => (p.usda.snapGrossSelfEmploymentIncomePerson * 12)) -
      t.usda.snapSelfEmploymentExpenseDeduction) : Rat) 0)

/-- `snap_utility_allowance.py`
    in `policyengine_us/variables/gov/usda/snap/income/deductions/shelter/`
    policyengine-us 1.783.0, entity spm_unit, value_type float. -/
def snapUtilityAllowance (t : TaxUnit) (d : Date) : Rat :=
  (t.usda.snapStandardUtilityAllowance
  + t.usda.snapLimitedUtilityAllowance
  + t.usda.snapIndividualUtilityAllowance)

/-- `policyengine_us/variables/gov/usda/wic/wic.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def wic (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (if p.usda.takesUpWicIfEligible then p.usda.wicIfTakesUp else 0)

/-- `policyengine_us/variables/gov/usda/wic/wic_food_package.py`
    policyengine-us 1.783.0, entity person, value_type Enum. -/
def wicFoodPackage (t : TaxUnit) (p : Person) (d : Date) : WICFoodPackage :=
  let pregnant := (p.usda.wicCategory == WICCategory.PREGNANT);
  let breastfeeding := (p.usda.wicCategory == WICCategory.BREASTFEEDING);
  let infant := (p.usda.wicCategory == WICCategory.INFANT);
  let age_months := (p.coreP1.age * 12);
  let fully_formula_fed := (p.usda.wicInfantFeedingCategory ==
      WICInfantFeedingCategory.FULLY_FORMULA_FED);
  let partially_breastfed := (p.usda.wicInfantFeedingCategory ==
      WICInfantFeedingCategory.PARTIALLY_BREASTFED);
  let fully_breastfed := (p.usda.wicInfantFeedingCategory ==
      WICInfantFeedingCategory.FULLY_BREASTFED);
  let child := (p.usda.wicCategory == WICCategory.CHILD);
  (if (pregnant && (decide (p.coreP1.currentPregnancies ≥ 2))) then
      WICFoodPackage.PREGNANT_MULTIPLE_FETUSES else (if ((pregnant &&
      p.coreP1.isBreastfeeding) && (decide (p.coreP1.currentPregnancies = 1))) then
      WICFoodPackage.FULLY_BREASTFEEDING else (if pregnant then
      WICFoodPackage.PREGNANT_SINGLETON else (if ((breastfeeding &&
      p.usda.isWicFullyBreastfeeding) && (decide (p.usda.wicBreastfeedingInfantCount ≥ 2)))
      then WICFoodPackage.FULLY_BREASTFEEDING_MULTIPLES else (if (breastfeeding &&
      (p.usda.isWicFullyBreastfeeding || (decide (p.usda.wicBreastfeedingInfantCount ≥ 2))))
      then WICFoodPackage.FULLY_BREASTFEEDING else (if breastfeeding then
      WICFoodPackage.PARTIALLY_BREASTFEEDING else (if (p.usda.wicCategory ==
      WICCategory.POSTPARTUM) then WICFoodPackage.POSTPARTUM_NON_BREASTFEEDING else (if
      (infant && (p.usda.wicInfantFeedingCategory == WICInfantFeedingCategory.AVERAGE)) then
      WICFoodPackage.INFANT_AVERAGE else (if ((infant && (decide (age_months < 4))) &&
      fully_formula_fed) then WICFoodPackage.INFANT_0_3_FULLY_FORMULA_FED else (if ((infant
      && (decide (age_months < 6))) && fully_formula_fed) then
      WICFoodPackage.INFANT_4_5_FULLY_FORMULA_FED else (if ((infant && (decide (age_months <
      4))) && partially_breastfed) then WICFoodPackage.INFANT_0_3_PARTIALLY_BREASTFED else
      (if ((infant && (decide (age_months < 6))) && partially_breastfed) then
      WICFoodPackage.INFANT_4_5_PARTIALLY_BREASTFED else (if (infant && partially_breastfed)
      then WICFoodPackage.INFANT_6_11_PARTIALLY_BREASTFED else (if ((infant && (decide
      (age_months < 6))) && fully_breastfed) then WICFoodPackage.INFANT_0_5_FULLY_BREASTFED
      else (if (infant && fully_formula_fed) then
      WICFoodPackage.INFANT_6_11_FULLY_FORMULA_FED else (if (infant && fully_breastfed) then
      WICFoodPackage.INFANT_6_11_FULLY_BREASTFED else (if (child && (decide (p.coreP1.age <
      2))) then WICFoodPackage.CHILD_12_23_MONTHS else (if child then
      WICFoodPackage.CHILD_24_59_MONTHS else WICFoodPackage.NONE))))))))))))))))))

/-- `policyengine_us/variables/gov/usda/has_all_usda_elderly_disabled.py`
    policyengine-us 1.783.0, entity spm_unit, value_type bool. -/
def hasAllUsdaElderlyDisabled (t : TaxUnit) (d : Date) : Bool :=
  (allBy t.members fun p => ((isUsdaElderly t p d) || p.usda.isUsdaDisabled))

/-- `policyengine_us/variables/gov/usda/has_usda_elderly_disabled.py`
    policyengine-us 1.783.0, entity spm_unit, value_type bool. -/
def hasUsdaElderlyDisabled (t : TaxUnit) (d : Date) : Bool :=
  (anyBy t.members fun p => ((isUsdaElderly t p d) || p.usda.isUsdaDisabled))

/-- `policyengine_us/variables/gov/usda/snap/eligibility/student/is_snap_ineligible_student.py`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSnapIneligibleStudent (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (if (isSnapHigherEdStudent t p d) then (!((((((decide
      ((usda.snap.student.age_threshold.atDate d p.coreP1.age) ≠ 0)) || p.coreP1.isDisabled)
      || (isSnapEmploymentTrainingOrWorkIncentiveStudent t p d)) || (meetsSnapWorkException
      t p d)) || p.usda.meetsSnapParentException) || ((decide (p.hhs.tanfPerson > 0)) ||
      (decide (boolToRat t.hhs.receivesTanf > 0))))) else false)

/-- `policyengine_us/variables/gov/usda/snap/income/snap_earned_income_person.py`
    policyengine-us 1.783.0, entity person, value_type float. -/
def snapEarnedIncomePerson (t : TaxUnit) (p : Person) (d : Date) : Rat :=
  (if (snapCountableEarner t p d) then (p.coreP1.employmentIncome / 12) else 0)

/-- `is_snap_state_discretion_ineligible_alien.py`
    in `policyengine_us/variables/gov/usda/snap/income/ineligible_members/`
    policyengine-us 1.783.0, entity person, value_type bool. -/
def isSnapStateDiscretionIneligibleAlien (t : TaxUnit) (p : Person) (d : Date) : Bool :=
  (((!(isSnapIneligibleStudent t p d)) && (!(isSnapImmigrationStatusEligible t p d))) &&
      ((usda.snap.income.ineligible_members.pre_prwora_statuses.atDate d).contains
      (ImmigrationStatus.asStr p.coreP1.immigrationStatus)))

end Lawlib.USA
