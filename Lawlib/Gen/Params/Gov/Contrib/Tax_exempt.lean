/-
Generated by pe2lean 0.1.0 from policyengine-us 1.783.0.
DO NOT EDIT — regenerate with `pe2lean params` (design §5).
-/
import Lawlib.Core.Money
import Lawlib.Core.Date
import Lawlib.Core.ExtRat
import Lawlib.Core.Scale

namespace Lawlib.Gen.Params
open Lawlib

/-- Various income sources are exempt from federal income or payroll tax, if this is true.
    `gov/contrib/tax_exempt/in_effect.yaml` (policyengine-us). -/
def gov.contrib.tax_exempt.in_effect : DatedParam Bool :=
  ⟨(⟨0, 1, 1⟩, false), []⟩

/-- The propsal to exempt overtime income from income tax applies, if this is true.
    `gov/contrib/tax_exempt/overtime/income_tax_exempt.yaml` (policyengine-us). -/
def gov.contrib.tax_exempt.overtime.income_tax_exempt : DatedParam Bool :=
  ⟨(⟨0, 1, 1⟩, false), []⟩

/-- The propsal to exempt overtime income from payroll tax applies, if this is true.
    `gov/contrib/tax_exempt/overtime/payroll_tax_exempt.yaml` (policyengine-us). -/
def gov.contrib.tax_exempt.overtime.payroll_tax_exempt : DatedParam Bool :=
  ⟨(⟨0, 1, 1⟩, false), []⟩

/-- The propsal to exempt tip income from payroll tax applies, if this is true.
    `gov/contrib/tax_exempt/tip_income/income_tax_exempt.yaml` (policyengine-us). -/
def gov.contrib.tax_exempt.tip_income.income_tax_exempt : DatedParam Bool :=
  ⟨(⟨0, 1, 1⟩, false), []⟩

/-- The propsal to exempt tip income from payroll tax applies, if this is true.
    `gov/contrib/tax_exempt/tip_income/payroll_tax_exempt.yaml` (policyengine-us). -/
def gov.contrib.tax_exempt.tip_income.payroll_tax_exempt : DatedParam Bool :=
  ⟨(⟨0, 1, 1⟩, false), []⟩

end Lawlib.Gen.Params
