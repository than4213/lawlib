import Lawlib

/-- M3 wires this up: read a JSON household from stdin, print the EITC
value. Stub until generated code exists. -/
def main : IO Unit := do
  IO.println "lawlib 0.1.0 — EITC evaluator (stub; JSON I/O lands at M3)"
