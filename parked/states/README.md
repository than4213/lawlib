# Parked: state / local / territory law (generated at pe2lean v0.8.6)

Lawlib's scope is **federal law** (decision 2026-08-03). This directory
preserves the state-level work as generated and partially validated at
v0.8.6 — 2,571 translated variables across 54 jurisdiction modules,
plus their parameters — **unimported and unbuilt**. It will drift from
the federal Trunk it references; regenerate with `pe2lean extract
--scope all` (pe2lean ≥ v0.9.0) when states become a priority again.

Known state of the validation tail at parking time: the child-care
subsidy chains (fl_sr_copay cluster) carried an unresolved
effective-date semantics question (see findings 19 investigation in the
session logs).
